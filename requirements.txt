# Core Discord bot
discord.py>=2.6.3
python-dotenv>=1.1.1

# Async HTTP + SQLite
aiohttp>=3.12.15
aiosqlite>=0.22.1

# Sync HTTP (ESPN API)
requests>=2.32.4

# Trade Evaluator — image generation (optional, gracefully disabled if missing)
Pillow>=11.3.0
openpyxl>=3.1.5
